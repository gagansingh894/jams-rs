[config]
protocol = "http"
port = 3000
model_store = "azure"
azure_storage_container_name = "jamsmodelstore"
poll_interval = 0 # in seconds, default off as polling reloads all the models and some models might require warmup which may affect latency.
num_workers = 4
