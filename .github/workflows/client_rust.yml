name: J.A.M.S Rust Client CI Pipeline

on:
  push:
    branches: [ "main" ]
    paths:
      - 'clients/rust/**'
  pull_request:
    branches: [ "main" ]
    paths:
      - 'clients/rust/**'

jobs:
  build:
    name: Lint and Test
    runs-on: ubuntu-latest

    container:
      image: rust:1.78
      options: --user root
      env:
        PROTOC: /usr/bin/protoc
        JAMS_HTTP_HOSTNAME: jams_http
        JAMS_GRPC_HOSTNAME: jams_grpc

    services:
      jams_http:
        image: gagansingh894/jams_http:latest
        ports:
          - 3000:3000
        env:
          TEST_MODEL_DIR: ${{ github.workspace }}/assets/model_store

      jams_grpc:
        image: gagansingh894/jams_grpc:latest
        ports:
          - 4000:4000
        env:
          TEST_MODEL_DIR: ${{ github.workspace }}/assets/model_store

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Protoc
        run: |
          echo "Installing protoc..."
          apt-get update
          apt-get install -y protobuf-compiler
          rm -rf /var/lib/apt/lists/*

      - name: Install cargo-nextest
        run: cargo install cargo-nextest

      - name: Lint
        run: make lint

      - name: Run tests
        run: make nextest